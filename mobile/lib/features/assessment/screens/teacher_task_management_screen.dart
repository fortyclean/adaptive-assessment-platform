import 'task_management_screen.dart';

/// Backward-compatible alias for the canonical teacher task management screen.
class TeacherTaskManagementScreen extends TaskManagementScreen {
  const TeacherTaskManagementScreen({super.key});
}
