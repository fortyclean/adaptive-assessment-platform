import 'package:flutter/material.dart';

import 'marketplace_screen.dart';

/// Backward-compatible alias for the student marketplace route.
class StudentMarketplaceScreen extends StatelessWidget {
  const StudentMarketplaceScreen({super.key});

  @override
  Widget build(BuildContext context) => const MarketplaceScreen();
}
