import 'package:flutter/material.dart';

import 'admin_dashboard_v2_screen.dart';

/// Screen 68 — Supervisor Dashboard
///
/// Uses the same approved enhanced admin dashboard layout to keep
/// navigation/actions consistent across all supervisor entry points.
class SupervisorDashboardScreen extends StatelessWidget {
  const SupervisorDashboardScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const AdminDashboardV2Screen();
  }
}
